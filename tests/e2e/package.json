{
  "name": "sellable-reserves-e2e",
  "private": true,
  "description": "Browser checks (flows, keyboard, axe-core WCAG audit) and an MCP agent test (official MCP SDK client, both protocol eras, plus an LLM agent via any OpenAI-compatible endpoint). Not needed to run the app.",
  "scripts": {
    "test": "node run.js",
    "mcp": "node mcp_agent.mjs"
  },
  "devDependencies": {
    "axe-core": "4.13.0",
    "playwright-core": "1.63.0",
    "@modelcontextprotocol/client": "2.1.0"
  },
  "type": "commonjs"
}
